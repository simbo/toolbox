.page--hash

  .spaced-section

    c-textfield(
      [(ngModel)] = "input",
      [multiline] = "true",
      [autosize] = "true",
      label = "Input",
      placeholder = "Paste your data here…"
    )

  .spaced-section

    c-checkbox(
      [(ngModel)] = "saltEnabled",
      label = "add Salt"
    )

    c-roller([expanded] = "saltEnabled")

      .spaced-section.is-follow-up

        c-textfield(
          [(ngModel)] = "salt",
          [multiline] = "true",
          [autosize] = "true",
          label = "Salt",
          placeholder = "Add some salt…"
        )

      .spaced-section

        .item-row

          c-select.item-row__item(
            [(ngModel)] = "saltPosition",
            [choices] = "saltPositionChoices",
            label = "Position"
          )

          c-textfield.item-row__item(
            [(ngModel)] = "saltSeparator",
            size = "tiny",
            label = "Separator"
          )

      .spaced-section

        c-textfield(
          [ngModel] = "inputWithSalt",
          [readonly] = "true",
          [multiline] = "true",
          [autosize] = "true",
          label = "Input with Salt",
          placeholder = "Nothing to display…"
        )

  .spaced-section

    .item-row

      c-select.item-row__item(
        [(ngModel)] = "algorithm",
        [choices] = "algorithms",
        label = "Algorithm"
      )

      c-select.item-row__item(
        [(ngModel)] = "encoding",
        [choices] = "encodings",
        label = "Encoding"
      )

  .spaced-section

    c-textfield(
      [ngModel] = "output",
      [readonly] = "true",
      [multiline] = "true",
      [autosize] = "true",
      label = "Output",
      placeholder = "Nothing to hash…"
    )
